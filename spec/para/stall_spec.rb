require 'spec_helper'

describe Para::Stall do
  it 'has a version number' do
    expect(Para::Stall::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
